{#
Title: Pillars
Description: A Custom Hero block
Category: formatting
Icon: <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="#fff" stroke="#000" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="feather feather-calendar"><rect x="3" y="4" width="18" height="18" rx="2" ry="2" fill="#fff"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
Keywords: Pillars Block
Mode: auto
Align: center
PostTypes: page post
SupportsAlign: left right center wide full
SupportsMode: true
SupportsMultiple: true
#}
<div id="pillars-{{block.id}}" class="pillars block {{block.classes}}">
  <div class="wrapper">
    <div class="pillars__inner">
      <div class="pillars__title">
        <h2>{{ fields.title }} <span class="pillars__title-colour">{{ fields.colour_title }}</span></h2>
        <p>{{ fields.description }}</p>
      </div>
      <div class="pillars__items">
        {% for item in fields.items %}
        <div class="pillars__item">
          <div class="pillars__item-icon">
            <img src="{{ item.icon.url }}" alt="{{ item.icon.alt }}" />
          </div>
          <div class="pillars__item-content">
            <h3>{{ item.title }}</h3>
            <p>{{ item.description }}</p>
          </div>
        </div>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
