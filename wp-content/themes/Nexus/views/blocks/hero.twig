{#
Title: Hero
Description: A Custom Hero block
Category: formatting
Icon: <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="#fff" stroke="#000" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="feather feather-calendar"><rect x="3" y="4" width="18" height="18" rx="2" ry="2" fill="#fff"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
Keywords: Hero Block
Mode: auto
Align: center
PostTypes: page post
SupportsAlign: left right center wide full
SupportsMode: true
SupportsMultiple: true
#}
<div id="hero-{{block.id}}" class="hero block {{block.classes}}">
  <div class="wrapper">
    <div class="hero__content">
      <div class="hero__content-inner">
        <h1>{{fields.title}} <span class="hero__colour-title">{{fields.colour_title}}</span></h1>
        <p>{{fields.description}}</p>
        <a href="{{fields.button_url}}" class="btn btn--primary">{{fields.button_text}}</a>
      </div>
    
    <div class="hero__image">
      {% if fields.image %}
        <div class="hero__image-inner">
          <img src="{{fields.image.url}}" alt="{{fields.image.alt}}" />
        </div>

        {% else %}
        <div class="hero__image-inner">
          <svg class="hero__image-deco" width="48" height="48" fill="none" style="position:absolute;bottom:16px;right:16px;opacity:0.08;">
    <circle cx="24" cy="24" r="20" stroke="#3b82f6" stroke-width="4"/>
  </svg>
        </div>
      {% endif %}
    </div>
    </div>
  </div>
</div>